---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F05 (invariant ids stable across project-form saves), F06 (Dashboard surfaces the fetch failure it captured), F13 (theme-safe text colour tokens + computed contrast guard), F12 (programmatic labels on every form control; drawer aria-expanded / Escape / focus return)
Files modified: claude-code-guide-v2.html, scripts/check-html.mjs, tests/mutation-audit.mjs, .cycle/config.md, CHANGELOG.md, VERSION

CHANGES:
F05 | claude-code-guide-v2.html (parseInvariantFormLines/saveProjectForm/openEditProject + the pf-invs label) | ids round-trip through the textarea as an optional `INV-NN |` prefix (the Cycle Workflow Config's own shape); existing ids are kept when a line is deleted; a new line is allocated above the max of BOTH stores (form invariants AND §4v-added custom ones), which is what removes the duplicate-id collision. Browser: initial INV-01..03; after deleting the middle line, INV-01=alpha and INV-03=gamma keep their text and the new line takes the next free number.
F06 | claude-code-guide-v2.html (refreshDashboard, dashboardCard, new dashErrHint, .dcard-err) | the error is now recorded on EVERY failure (it was dropped whenever a cache entry already existed) and rendered on the card with the fix for 404 / 401-403 / network+file:// cases; the status line says "N failed — reason on the card". Browser: a card with a failed fetch reads "🔴 Fetch failed ⚠ …" instead of "No data yet".
F13 | claude-code-guide-v2.html (CSS tokens + 16 literal call sites, scoreColor, setStateIoMsg, renderCycle, project-form title), scripts/check-html.mjs | text colour goes through --on-green/amber/red/blue/purple/teal, which flip; the semantic FILLS stay put so the deliberate "chrome only" light theme is unchanged. The Dashboard score chip flips too (S5 named it as the risk that decision left standing). The four dead `.fill-select` rules the finding named are now live: the select carried `.fill-input` plus inline styles duplicating them, and its `<option>` background rule was dead and un-themed. Browser: nav badge #86efac→#166534, flow chip #5eead4→#115e59, state message dark-grey literal→--text-sec, all flipping with the theme.
F12 | claude-code-guide-v2.html (markup labels, buildFillForm, renderSubRows, dashboardCard, setNavOpen/toggleNav/closeNav + Escape listener), scripts/check-html.mjs | every form control carries `<label for>` or an aria-label; the drawer toggle has aria-expanded/aria-controls, Escape closes the drawer and returns focus to the toggle, and the restore is guarded on the drawer having been open (the listener is document-wide and the toggle is display:none on desktop). Browser at 375px: aria-expanded true→false, focus moves into the drawer on open and back to the toggle on Escape, 27 of 27 controls labelled; on desktop Escape leaves focus untouched.
GUARDS | scripts/check-html.mjs, tests/mutation-audit.mjs, .cycle/config.md | INV-59..62 added (62 total, 62 runnable). The element double now tracks classList, attributes and focus instead of no-oping them, and document listeners are recorded so the drawer assertions DRIVE the behaviour rather than reading it. F13's contrast half is computed (48 token/surface pairs) rather than eyeballed.

TEST RESULTS: passed — full 16-stage Test Command green (283 ✓); invariant-check 62/62 runnable PASS, 0 manual; mutation audit 62/62 proven fail-closed across 70 mutations (was 64). Browser (headless Chromium, animations disabled) verified all four fixes plus the two behaviour changes I introduced (the .fill-select swap renders with 14 options, sans font and its arrow; desktop Escape is inert). Regression Scenarios: S1–S4 covered by check-html's driven paths; S5/S7/S8 remain operator walks and now have concrete things to confirm.
REGRESSION RISKS: (1) TWO DELIBERATE VISUAL CHANGES for S5/S8 to confirm — the subsystem dropdown moves from monospace 12px to sans 12.5px now that `.fill-select` applies, and the Dashboard score chip is lighter in dark mode (--on-green vs --green) and dark in light mode. (2) The pf-invs textarea format changed: a plain list still works (no id → allocated), but a rule whose text is EXACTLY "INV-NN" would be consumed as an id. (3) Invariant ids now have gaps after a deletion (INV-01, INV-03) — intended; stability beats density. (4) Rule text containing a pipe still mis-splits in the form (PRE-EXISTING, unchanged by this batch; the §4v pack's reader is permissive and unaffected). (5) The Escape listener is document-wide; asserted inert when the drawer is closed.
INVARIANTS AT RISK: INV-37 (scoreColor now returns --on-* tokens; its "green"/"red" assertions still hold and the mutation case still fires), INV-20/INV-53 (buildFillForm markup changed — the derived sink scan and all 7 field-level cases re-pass), INV-49 (the backdrop exemption is now CONDITIONAL on the Escape path; mutation-proven), INV-42 (showPanel/nav unchanged, re-proven), INV-56 (:focus-visible untouched), INV-08/INV-15 (invariant rendering unchanged). None failing.
NET SCORE: 4 − 0 = 4 (production fixes F05, F06, F13, F12 — the last three are user-visible interface defects, which /reflect Q1 counts YES; defensive/structural: 1, the test-double and exemption hardening that came with them)

OPERATOR ACTIONS / DEPLOY:
- None for consumers: console + tooling only, no command body or config-schema change, so no /sync-commands re-pull | BLOCKS DEPLOY: N
- Walk S8 (light-mode chips) and S5 after Pages republishes — the ratios are computed, the look is not; both visual changes above are the specific things to eyeball | BLOCKS DEPLOY: N
Deploy: Interactive Console (HTML): merge to `main` — GitHub Pages republishes from index.html; browser-verify AFTER. Canonical Templates & Docs: no deploy. Tooling & Sync Infrastructure: no deploy (CI-only).

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- deleteProject() leaves `ccg:<pid>:invariants`, `:archive`, `:cycle` and fill values behind; a new project deriving the same id inherits a dead project's invariants (and F05's allocator then skips those numbers). Out of scope here — noticed while reading the id allocator.
- The project form splits invariant lines on `|`, so rule text containing a pipe mis-parses (this repo's own INV-08/INV-46 would). Pre-existing; the §4v pack reader is already permissive for the same reason.
- `--accent` (#4f8ef7) is 3.4:1 on the light surface and is used for links, section labels and the prompt-block label. Left alone deliberately: it is the console's identity colour and the "chrome only" decision covers it. Worth a decision in a later batch, not a silent flip.
- The `#fff` phase-dot label on a semantic fill is 2.3:1 (now `--fg-on-solid`). Same in both themes, so it is not a theming defect — it is a perceptual one for S8.
- Batch 5 (F11, F15, F14) and Batch 6 (F16) remain from the Cycle-6 batch plan.

DOCUMENTATION UPDATES NEEDED:
- .cycle/config.md Common Gotchas: two from this batch — an exemption pinned to a call's SYNTAX (`closeNav()`) silently stops matching when the call changes, so tie an exemption to the condition that justifies it; and `\bfor=` matches `data-for=`, the kind of near-miss regex that makes a guard read as passing.
- README/CLAUDE.md: no change needed (no command, schema or script-inventory change).
---END BROAD SCAN IMPLEMENTATION SUMMARY---
