---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: archive-copy bypass (live defect from §4v — F05's bug alive
in one sink), INV-53 (hostile fixture payload set hand-picked → the false green
§4v found), INV-55 (console block displays had no shape check), INV-56 (structural
half of focus visibility), F15 (CI permissions block unasserted).

Files modified: claude-code-guide-v2.html, scripts/check-html.mjs,
scripts/check-output-blocks.mjs, scripts/check-template-sync.mjs,
tests/guard.test.mjs, .cycle/config.md, CHANGELOG.md, VERSION

CHANGES:
archive-copy | claude-code-guide-v2.html, check-html.mjs | Button routed through
  copyToClipboard(); copyFeedback() restores each button's own label instead of
  hardcoding the prompt-copy icon (the reason the helper could not be reused
  there); INV-54 static scan added.
INV-53 | check-html.mjs | Payload set DERIVED from the sinks' ${obj.field}
  interpolations, alias-scoped, reporting both derived and held-back fields;
  archive sinks now rendered under the fixture.
INV-55 | check-output-blocks.mjs | New validateHtml(); all 12 blocks shape-checked
  in the console, container punctuation stripped before comparison.
INV-56 | claude-code-guide-v2.html, check-html.mjs | Global :focus-visible using
  box-shadow; check asserts coverage of every outline:none suppression.
F15 | check-template-sync.mjs, tests/guard.test.mjs | Structural check 8 +
  fail-closed case; guard.test setup() now copies .github/.

TEST RESULTS: passed — full 14-stage Test Command exit 0; invariant-check 45/45
runnable PASS (was 40), 0 FAIL, 12 MANUAL, 57 total; guard.test 15/15.

REGRESSION RISKS:
- copyFeedback() now reads/writes btn.dataset.origLabel; guarded for stub buttons
  without dataset. First caller whose label is dynamic would cache the first
  label seen — none exist today.
- The :focus-visible rule is global and applies to every focusable element,
  including form controls that previously showed no ring. Intended; visually
  unverified (S7).
- INV-55 skips blocks the console does not render rather than failing — presence
  remains check-template-sync's derived job, so the two checks do not overlap.

INVARIANTS AT RISK: None violated. INV-20's scope-limit note retired because the
gap it recorded is closed. INV-53..57 added, all mutation-proven.

NET SCORE: 2 − 0 = 2
(2 production fixes: the archive-copy silent failure (Medium, live) and the
focus-visibility gap (Medium — 15 inline outline:none controls had no indicator).
0 new capabilities. 3 defensive/structural: INV-53 derivation, INV-55 console
shape check, F15 assertion.)

OPERATOR ACTIONS / DEPLOY:
- Walk S7 (keyboard-only) after deploy — the focus indicator now EXISTS and is
  guarded structurally, but whether its contrast is adequate is perceptual and
  only S7 can answer | BLOCKS DEPLOY: N
Deploy: Interactive Console — merge to `main`; GitHub Pages republishes.

FOLLOW-ON ITEMS:
- legacyCopy()'s success path is still unexercised (stub execCommand returns false).
- The 12 MANUAL invariants have never been audited for correctness.
- The F11 mutation audit is still in scratchpad; §4v proved it needs a field-level
  tier, which INV-53 now provides for escaping but not generally.
- CI/Test-Command stage parity is checked by hand, not asserted (noticed while
  adding F15's check 8; out of scope for this batch).
- S5/S6/S7 have still never been walked in a browser.

DOCUMENTATION UPDATES NEEDED: None — CHANGELOG, VERSION and the invariant library
were updated in this session.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
