---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F11 (fill form offers operator placeholders and only those), F14 (§4v rotation probes seeded and reproducible instead of re-rolled), F15 (.cycle/STATE.md restored to its own template; history split out), F16 (every static console prompt locked to a canonical body)
Files modified: claude-code-guide-v2.html, CLAUDE.md, .claude/commands/setup-cycle.md, scripts/gen-html-prompts.mjs, scripts/check-html.mjs, scripts/check-template-sync.mjs, tests/mutation-audit.mjs, .cycle/STATE.md, .cycle/HISTORY.md (new), .cycle/config.md, README.md, CHANGELOG.md, VERSION

CHANGES:
F16 | scripts/gen-html-prompts.mjs, CLAUDE.md (new "Console Reference Prompts" section), claude-code-guide-v2.html, .claude/commands/setup-cycle.md | THE AUDIT UNDER-COUNTED THIS ONE. It listed eight unlocked console prompts; there were nine, and the ninth was `setup` — which DOES have a canonical counterpart and had decayed to a pre-R18 copy missing 36 canonical lines, including the whole "user-facing surfaces" profile step and the instruction to propose an interface Health Dimension. An operator running /setup-cycle FROM THE CONSOLE got a config that could never score the interface layer, which is the exact gap R18 shipped in v1.19.0 to close; the file-global markers could not see it because those phrases appear elsewhere in the file. The static MANIFEST now resolves a canonical body from either a slash `command:` or a `section:` heading (the same resolver the dynamic lock uses); all 16 static <pre> blocks are locked, the nine with no slash command have canonical bodies under "Console Reference Prompts", and --assert additionally FAILS if a new static prompt appears with no manifest entry. Three elaborations canonical had lost but the console kept shipping are restored to /setup-cycle (policy-threshold maturity guidance, the seam-files → Seams audit pointer, "for any Medium or Low, explain what you'd need to verify"), and p7tmpl is now project-agnostic instead of naming one built-in project's dimensions.
F11 | claude-code-guide-v2.html (PH_RE, outputBlockSpans, getPlaceholders, isFillField), scripts/check-html.mjs | fill fields and output-format tokens are now told apart STRUCTURALLY: a token inside an ---OUTPUT BLOCK--- span, or sharing its line with another bracket token, is part of a template the agent must emit. The old ALL-CAPS-only pattern missed every operator placeholder with a lowercase clause after an em dash while offering [ID], [INV-XX] and [X/10] — filling one rewrote the block the prompt tells the agent to produce. Across all 16 prompts: 31 fields offered, every one an operator input; §1 went from 3 to its full 5.
F14 | claude-code-guide-v2.html (probeSeed/hash32/selectSeededInvariants; selectRandomInvariants and doCopyVerification removed), CLAUDE.md + console §4v body, scripts/check-html.mjs | probes are a pure function of a stated seed (project + UTC day) instead of Math.random() re-rolled on every Copy — the copied prompt used to differ from the displayed one, so "do NOT substitute your own picks" had no force. R19 gave the script this property via the commit sha; the browser has none, so the prompt states its seed. Canonical wording updated from "selected at copy time" to "selected deterministically from a stated seed".
F15 | .cycle/STATE.md, .cycle/HISTORY.md (new), CLAUDE.md (Cycle State & Memory), scripts/check-template-sync.mjs (structural check 11) | STATE.md is back to its own 7-section template (347 lines → 64); 20 narrative sections moved to .cycle/HISTORY.md. It had two "Decisions made" AND two "Where I left off", and both the SessionStart hook and /cycle-resume read the FIRST match of a heading — so the next session would have loaded a superseded "Where I left off". The guard derives the expected headings from the template in CLAUDE.md and fails on an extra, missing or duplicated section.

TEST RESULTS: passed — full 16-stage Test Command green (290 ✓); invariant-check 66/66 runnable PASS, 0 manual; mutation audit 66/66 proven fail-closed across 74 mutations (was 70). Browser (headless Chromium): all 23 <pre> render non-empty with no unresolved ${} or "undefined" after --write rewrote 16 of them; Setup carries the R18 lines; p7tmpl is project-agnostic; §1's fill form shows exactly the 5 operator placeholders, all labelled; §4v states seed obs:2026-09-03, renders identically twice and is unchanged after pressing Copy, with 5 probes. Regression Scenarios: S1–S4 covered by driven paths; S5/S7/S8/S9 remain operator walks.
REGRESSION RISKS: (1) `--write` rewrote all 16 static prompts. Eight were already byte-identical to their new canonical bodies; `setup` gained 36 canonical lines and kept its three merged elaborations; p7tmpl was rewritten deliberately. Nothing else changed, but this is the largest single console text change in the repo's history and it is worth an eyeball. (2) F11's classification would drop a genuine operator placeholder that shares a line with another bracket token — none exists across the 16 prompts today, and the guard's rule (a) fails if one appears unoffered. (3) §4v probes now rotate once per UTC day, so a verifier reproducing a selection must use the same date; the prompt states the seed. (4) .cycle/HISTORY.md is new and nothing reads it — it is reference prose, deliberately not substrate.
INVARIANTS AT RISK: INV-29 (static §-prompt lock — now covers 16 rather than 7, re-proven), INV-36 (dynamic builders untouched, 9/9 still at 100% coverage), INV-16 (the /setup-cycle schema parity check now compares a console copy GENERATED from canonical, which strictly strengthens it — re-proven), INV-28 (the transform engine's exports are unchanged; commandBody/sectionBody/dynBody all still exported and tested), INV-31/INV-38/INV-55 (the new vblockref canonical body adds a third VERIFICATION BLOCK span in CLAUDE.md — all required fields present, shape checks pass), INV-24 (the SessionStart hook reads the restructured STATE.md correctly — verified by running it), INV-52 (the §4v pack still assembles; readBlocks now sees a third Cycle-6 block). None failing.
NET SCORE: 4 − 0 = 4 (production fixes F16, F11, F14, F15 — all four are user-visible defects on paths an operator uses, which /reflect Q1 counts YES; defensive/structural: 1, the derived lock-coverage assertion, which prevents rather than fixes). HONEST NOTE FOR /reflect: F15 is the borderline one. The underlying drift accumulated over five cycles, but the specific duplicate-heading instance that would have misled the next session was introduced by ME in v1.28.0's checkpoint two hours earlier. Count it however that reads best.

OPERATOR ACTIONS / DEPLOY:
- Consuming projects: run /sync-commands — /setup-cycle changed (three restored elaborations). No other command body changed | BLOCKS DEPLOY: N
- Projects that generated a Cycle Workflow Config from the CONSOLE's Setup prompt before v1.29.0: that prompt never asked about user-facing surfaces or proposed an interface Health Dimension, so re-check whether your config needs one (the same re-check v1.25.0 asked for on Seams Audit Cadence / Verify) | BLOCKS DEPLOY: N
- Walk S5/S8/S9 after Pages republishes — 16 prompts were regenerated and two visual changes from v1.28.0 are still unwalked | BLOCKS DEPLOY: N
Deploy: Interactive Console (HTML): merge to `main` — GitHub Pages republishes from index.html; browser-verify AFTER. Canonical Templates & Docs: consumers pick up /setup-cycle via /sync-commands. Tooling & Sync Infrastructure: no deploy (CI-only).

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- The §4v canonical body and the vblockref reference now both carry the VERIFICATION BLOCK shape, and Handoff Block Formats carries a third, shorter copy. All three are shape-checked, but they are three copies of one contract — a candidate for the same treatment R14 gave the prompts.
- ROADMAP R20 (real-DOM console test stage) is still the largest open gap: F17's fix is pinned by a static CSS rule, and the Chromium driver that found it lives in a session scratchpad rather than the repo.
- Carried from earlier batches, none addressed here: deleteProject() leaves per-project keys behind; the project form mis-parses rule text containing a pipe; --accent is 3.4:1 on the light surface; the phase-dot label is 2.3:1; legacyCopy()'s success path is unexercised.
- .cycle/HISTORY.md is append-only and nothing prunes it.

DOCUMENTATION UPDATES NEEDED:
- .cycle/config.md Common Gotchas — FOUR now owed, two carried from v1.28.0 and two from this batch: (1) an exemption pinned to a call's SYNTAX stops matching when the call changes; (2) `\bfor=` also matches `data-for=`; (3) a non-avalanching hash (FNV-1a) with the seed APPENDED shifts every value by the same constant and leaves the sort order untouched — seed as a prefix, and require a one-character seed change to reorder; (4) an audit's own count of "how many are outside the guard" can be wrong, so derive the set from the artifact rather than trusting the finding's list (this batch found nine where the scan said eight, and the extra one was the consequential one).
- README script inventory does not mention .cycle/HISTORY.md.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
