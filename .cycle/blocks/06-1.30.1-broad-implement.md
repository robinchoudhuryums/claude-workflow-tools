---BROAD SCAN IMPLEMENTATION SUMMARY---
NOTE ON PROVENANCE: this block is a BACKFILL, written on 2026-09-04 for a release that shipped without
one — which is the gap INV-72 now closes. It is NOT a transcript of that session. Everything below is
either quoted from the v1.30.1 CHANGELOG entry and commit message, or independently re-verified in the
backfilling session; the verification lines say which. Treat the CHANGES section as reconstructed and
the TEST RESULTS section as re-derived.

Findings implemented: dead-artifact removal — 26 dead CSS rules (17 classes) in the console, plus ROADMAP R21 queued
Files modified: claude-code-guide-v2.html, ROADMAP.md, CHANGELOG.md, VERSION, .cycle/STATE.md

CHANGES:
DEAD-CSS | claude-code-guide-v2.html | 26 rules across 17 classes removed, none applied to anything: `.log-card`, `.log-hdr`, `.log-ttl`, `.log-meta`, `.log-date`, `.lsh3`, `table.lt`, `.fr`, `.fn2`, `.fs`, `.fp0/1/2`, `.ft`, `.fl` (residue of an older archive rendering — renderArchive() builds entries with inline styles now; `.log-body` is still used and stayed); `.ct-check` and its two `.done` rules (superseded by the phase dots renderCycle() builds inline — Cycle-6 Batch 4 had tokenised the colour inside `.ct-check.done::after` without noticing the class is applied to nothing); `.prose`.
R21 | ROADMAP.md | queues the /broad-scan dead-artifact lens for Cycle 7 with its exact replacement text: SAFE (mechanically verified) vs PROPOSE (dynamic references cannot be ruled out), where PROPOSE is a question for the operator and never a removal. Deliberately NOT a simplification lens.

TEST RESULTS: passed. Re-verified independently in the backfilling session rather than taken from the
commit message: the full 16-stage Test Command is green at this commit (298 ✓ after the v1.31.0 work;
292 ✓ at v1.30.1 itself), and every one of the 17 removed classes has ZERO class-attribute uses in the
current file — checked against real `class="…"` attributes, `classList` calls and template-literal
forms, not raw text. The five textual occurrences of "prose" are the English word inside prompt bodies
and comments. Driving the console in headless Chromium at this commit produced results byte-identical
to v1.30.0 across every probe (188 ids / 0 duplicates, 25 prompts non-empty, Tier 1 intact, 20 nav
links resolving, 57 controls labelled, mobile stacked at 375px, drawer Escape/focus-return).
REGRESSION RISKS: None found. The original commit message notes that a RAW TEXT search reported
fl/fr/fs/ft/lt as "used" because they appear inside "flex", "from", "restoring" and `&lt;` — a
12-of-23 false-positive rate, which is why R21 widens the noun rather than the mandate. The applied set
was derived from real class attributes, and a rule was removed only when EVERY class in its selector
was dead.
INVARIANTS AT RISK: INV-04/INV-05 (script parses, no unresolved ${} — green), INV-42/INV-44 (nav and
panels untouched — 20/20 resolving), INV-56 (the :focus-visible rule was not among the removals). None
failing.
NET SCORE: 0 − 0 = 0 (defensive/structural: 1. No production behaviour changed — that is the claim the
verification above exists to support.)

OPERATOR ACTIONS / DEPLOY:
- None for consumers: no command body, prompt or config-schema change | BLOCKS DEPLOY: N
Deploy: Interactive Console (HTML): merge to `main` — GitHub Pages republishes from index.html.
Canonical Templates & Docs: no deploy. Tooling & Sync Infrastructure: no deploy (CI-only).

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- ROADMAP R21 (the /broad-scan dead-artifact lens) is queued as Cycle 7's first item. It changes a
  command body, so consuming projects will need a /sync-commands re-pull when it lands.
- This release shipping with no block is what INV-72 now guards; the gap was found by §4v, which could
  not see the release at all because §4v reads .cycle/blocks/ and not the CHANGELOG.

DOCUMENTATION UPDATES NEEDED:
- None beyond the CHANGELOG entry that shipped with the release.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
