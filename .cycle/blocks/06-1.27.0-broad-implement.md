---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F01 (fill-form XSS sink escaped), F09 (hostile-fixture sinks derived + interaction renders driven), F17 (mobile layout stacks), F04 (malformed stored project repaired/rejected instead of bricking init), F02 (verification pack scoped to the cycle), F03 (one quote-aware CSV parser for every metrics.csv reader + /reflect quoting rule), F10 (/cycle-init carries a project-agnostic PROJECT_HEALTH skeleton), F07 (PROJECT_HEALTH Current Standing corrected), F08 (config.md cycle-number/test-count drift)
Files modified: claude-code-guide-v2.html, scripts/check-html.mjs, scripts/csv.mjs (new), scripts/render-metrics.mjs, scripts/verification-pack.mjs, scripts/portfolio-status.mjs, tests/verification-pack.test.mjs, tests/portfolio-status.test.mjs, tests/mutation-audit.mjs, CLAUDE.md, .claude/commands/reflect.md, .claude/commands/cycle-init.md, PROJECT_HEALTH.md, .cycle/config.md, README.md, CHANGELOG.md, VERSION

CHANGES:
F01 | claude-code-guide-v2.html (buildFillForm) | every stored value / subsystem name / label goes through esc(); every handler argument (pid, placeholder name) through jsArg(). Chromium re-check: neither the option-attribute nor the textarea payload executes.
F09 | scripts/check-html.mjs, tests/mutation-audit.mjs | hostile-fixture sink set DERIVED from render writes (writeLog) instead of a hand list; static <pre> text loaded from markup, a hostile saved value seeded per placeholder, every fill form + the project editor driven; two fail-closed floors (interaction sinks reached; every init-time innerHTML sink present). 4 new field-level INV-20 mutation cases (option attr/text, both textarea branches, input value). First run of the derivation had the marker after switchProject and went green on three existing cases — the audit caught it; the floor now guards that.
F17 | claude-code-guide-v2.html (CSS @media), scripts/check-html.mjs | body{flex-direction:column} inside the mobile media query; static check pins the rule. Chromium: mobilebar [0,0,360,51], main [0,51,360,963], scrollWidth 360 at 375px; 768px likewise.
F04 | claude-code-guide-v2.html (normalizeProject/loadCustomProjects/stateBackupKeys + 2 messages), scripts/check-html.mjs | missing arrays/strings repaired, entries without id/name dropped (one console.warn); a backup whose ccg:customProjects is not a JSON array is refused whole (reason 'bad-projects') with a visible message in Import and Load ← repo; fresh-context boot test. Chromium: no load error, project listed with Delete.
F02 | scripts/verification-pack.mjs, tests/verification-pack.test.mjs | readBlocks(dir,{cycle}) reads only `<cycle>-` blocks (zero-padded or bare) and returns .excluded; the pack names the excluded other-cycle blocks; no cycle = old behaviour. 3 new test assertions.
F03 | scripts/csv.mjs (new), render-metrics.mjs, verification-pack.mjs, portfolio-status.mjs, CLAUDE.md /reflect, tests | parseRow/parseCSV in one module; cycleTotals and netTrend parse quote-aware (comma-bearing subsystem rows are now counted); render-metrics's private parser retired; INV-25 mutation case follows the move; /reflect METRICS says to double-quote any comma-bearing field; fixtures gained "Auth, Security & HIPAA" rows.
F10 | CLAUDE.md (/cycle-init step 5 + Cycle State bullet), .claude/commands/cycle-init.md | inline project-agnostic skeleton with the field labels the parsers read; the "§7 in the HTML tool" pointer replaced.
F07 | PROJECT_HEALTH.md Current Standing | three stale lines corrected (focus-visible now present; INV-20 false green closed; archive Copy bypass closed in v1.24.0); block marked as LIVE STATUS.
F08 | .cycle/config.md | INV-07/INV-10 "Cycle 6" → "v1.25.0 (Cycle 5, post-synthesis)"; tests/ count; scripts/csv.mjs added to the Tooling subsystem.

TEST RESULTS: passed — full 16-stage Test Command green (272 ✓); invariant-check 58/58; mutation audit 58/58 proven fail-closed across 64 mutations (was 60). New guards additionally proven by 9 hand mutations of the fixes + 1 of the guard's own marker. Browser (headless Chromium, animations disabled): F01 payloads no longer execute; F04 boot clean with the custom project listed; F17 geometry stacked at 375px and 768px. S1–S4 not re-walked by hand (their code paths are covered by check-html); S5–S7 remain operator walks.
REGRESSION RISKS: (1) loadCustomProjects now normalizes (and JSON-compares) on every call and getProject() calls it per render — negligible for a handful of projects, but it is extra work on a hot path. (2) readBlocks with a cycle excludes any block whose name lacks a numeric prefix (e.g. notes.md) — reported in the pack, not silent. (3) A backup with a malformed ccg:customProjects is now refused WHOLE where before it half-imported; deliberate, message states it. (4) Existing metrics.csv rows in consuming projects with UNQUOTED comma subsystems still shift columns — the parser reads quoted rows correctly but cannot repair unquoted ones.
INVARIANTS AT RISK: INV-20 (strengthened, 7 cases), INV-53 (derivation untouched, still asserted), INV-25 (parseCSV moved to scripts/csv.mjs — mutation case updated, proven), INV-52 (pack derivation — readBlocks now cycle-scoped, tests extended), INV-10/INV-48 (import validation gained a reason — all clauses still pass), INV-12/INV-18 (reflect METRICS step still gated), INV-29 (static prompts regenerated, --assert green), INV-23 (1.27.0 both sides). None failing.
NET SCORE: 6 − 0 = 6 (production fixes F01, F17, F02, F03, F10, F07 — F07 is a corrected live-status surface and borderline for /reflect to rule on; defensive/structural: F04, F09, F08)

OPERATOR ACTIONS / DEPLOY:
- Consuming projects: run /sync-commands to pick up v1.27.0 (/reflect and /cycle-init bodies changed) | BLOCKS DEPLOY: N
- Consuming projects whose metrics.csv already has UNQUOTED comma-bearing subsystem rows: quote them once by hand (the parser cannot repair a shifted row) | BLOCKS DEPLOY: N
- Walk S9 (phone layout) and S5 in a browser after Pages republishes — the geometry is machine-verified, the look is not | BLOCKS DEPLOY: N
Deploy: Interactive Console (HTML): merge to `main` — GitHub Pages republishes from index.html; browser-verify AFTER. Canonical Templates & Docs: no deploy (consumers /sync-commands). Tooling & Sync Infrastructure: no deploy (CI-only).

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- claude-code-guide-v2.html: the "files" fill branch (LIST RELEVANT FILES AND DIRECTORIES) renders for no prompt — no generated prompt carries that placeholder, so fillSubsystem/"Use ↗" save a value nothing displays (relates to F11, Batch 5).
- A real-DOM test stage (Stage 3 gap): F17 is pinned by a static CSS check only; the Chromium driver used for verification lives in the session scratchpad, not the repo.
- loadCustomProjects: memoize the normalized list per storage write rather than re-normalizing on every getProject().
- .cycle/blocks/: the nine Cycle-5 blocks are now excluded from packs but still listed every run; consider an archive/ subfolder convention.
- F15 (STATE.md back to template shape) — Batch 5.

DOCUMENTATION UPDATES NEEDED:
- .cycle/config.md Common Gotchas: add the four from the Cycle-6 scan (interaction-only renders are invisible to init-time checks; bare-comma CSV readers; .cycle/blocks accumulates across cycles; PROJECT_HEALTH Current Standing is live status) and note that the last hand-listed guard set is gone.
- CLAUDE.md "Cycle State & Memory" verification-pack bullet: mention that blocks are read by cycle prefix.
- Operator State Checklist (.cycle/config.md): v1.27.0 changed /reflect and /cycle-init → re-pull.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
