---BROAD SCAN IMPLEMENTATION SUMMARY---
NOTE ON ORDER: this block is POST-REFLECT, POST-§4v, POST-SEAMS remediation. It implements Batches 1+2
of the batch plan built from the §4v VERIFICATION BLOCK and the SEAMS & INVARIANTS AUDIT BLOCK. /reflect
already ran for Cycle 6 and is the sole writer of net_score/prod_fixes/new_failure_modes, so nothing here
is in the cycle-6 metrics row.

Findings implemented: A1 (INV-68 invariant-library parse floor), A2 (library text repairs: INV-50 rescoped, INV-66 de-contradicted, INV-29 retired as subsumed), B1 (INV-69 the covering :focus-visible rule must APPLY, not merely exist), B2 (INV-70 the §4v pack's disclosed seed must reproduce its own printed probes)
Files modified: scripts/check-template-sync.mjs, scripts/check-html.mjs, scripts/verification-pack.mjs, tests/verification-pack.test.mjs, tests/mutation-audit.mjs, claude-code-guide-v2.html, .cycle/config.md, CHANGELOG.md, VERSION

CHANGES:
A1 | scripts/check-template-sync.mjs (structural check 12), .cycle/config.md, tests/mutation-audit.mjs | Two parsers read the same invariant library and disagreed: invariant-check.mjs anchors on `^(INV-\d+)` with no leading-whitespace tolerance; verification-pack.mjs trims first. A rule written with ONE leading space vanished from invariant-check AND from the mutation audit (which derives its set from that same parse, so no case was orphaned and nothing failed), while the §4v pack listed it. Both reported "67/67 … Every runnable invariant fails closed ✓" against a 68-rule file — proven by adding such a rule. Check 12 measures both parsers against a permissive count of the file itself and fails if either sees fewer or they disagree, naming the dropped ids. INV-58 floors the mutation-case dimension; this floors the parse a level up. The two readers are imported dynamically and defensively — a consuming project with a .cycle/ library but without those modules gets a reported skip, not a crash (verified by removing the module).
A2 | .cycle/config.md, tests/mutation-audit.mjs | INV-50 now states the scope it proves (an id a render writes to is PRESENT; it does NOT prove getElementById resolves to the intended element — both Cycle-6 duplicate-id defects passed under it). INV-66's sentence claimed nine prompts had no slash-command counterpart while naming `setup`, which had one, among them; now: nine sat outside every lock, eight without a counterpart. INV-29 RETIRED — it enumerated 7 of the 16 locked static prompts and is subsumed by INV-66; its DRIFT mutation was moved onto INV-66 rather than retired with it, so INV-66 now carries two cases (unlocked-new-prompt + drift).
B1 | scripts/check-html.mjs, claude-code-guide-v2.html (CSS comment), .cycle/config.md, tests/mutation-audit.mjs | INV-56 asserted that SOME :focus-visible rule using box-shadow existed somewhere in the file. Scoping the file's single rule to `.nav-item` strips the indicator from all 19 inline-suppressed form controls and the check still called every one "covered". Coverage was real but rested on the rule being UNIVERSAL — an unstated property nothing asserted. INV-69 asserts it. Confirmed in Chromium FIRST that the universal rule genuinely reaches a control carrying inline outline:none (box-shadow 0 0 0 3px on focus), so the guarded contract matches observed behaviour rather than a guess. The stale count in the rule text and in the CSS comment ("15 of the 17"; there are 19) is replaced by a derived count.
B2 | scripts/verification-pack.mjs, tests/verification-pack.test.mjs, .cycle/config.md, tests/mutation-audit.mjs | main() selected probes with the full 40-char HEAD sha and disclosed seed.slice(0,12) — in both the "seeded from" line and the `--seed` reproduce command — so a verifier following the pack's own instruction got a DIFFERENT five and §4v's "do NOT substitute your own picks" was unauditable. Proven at the last pack's own commit: the full sha yields the five it printed (INV-38,13,02,27,17), the printed 12-char seed yields five others (INV-58,07,39,37,44). The seed is now resolved once at the source (`git rev-parse --short=12 HEAD`) and never post-processed, and buildPack DERIVES the probes from the seed it prints, accepting no `probes` override — an override reopens exactly this divergence. The old test asserted determinism and the seed's presence separately, passing an already-truncated seed alongside probes from a different string, and never the composition; the new assertion re-derives from the built pack, including a >12-char seed (a seed of 12 or fewer characters cannot expose a truncation bug).

TEST RESULTS: passed — full 16-stage Test Command green, 298 ✓ (was 292); invariant-check 69/69 runnable PASS, 0 manual; mutation audit 69/69 proven fail-closed across 78 mutations (was 75). Each new guard was additionally proven by hand against the exact vector that motivated it: one leading space on an INV- line (caught, names INV-40), the covering rule scoped to .nav-item (caught by INV-69, INV-56 correctly still passes — no cross-crediting), the rule deleted entirely (caught by INV-56, INV-69 correctly silent), and the seed truncation reintroduced (caught by INV-70). Rebased onto origin/main (v1.30.1) BEFORE committing and re-ran the whole suite there — the branch was based on a stale main and would otherwise have reverted the dead-CSS release.
REGRESSION RISKS: (1) buildPack's signature changed — the `probes` parameter is REMOVED, not deprecated. Only this repo's main() and its test call it, both updated; any external importer would break loudly rather than silently. (2) The default seed is now a 12-char short sha, so a pack generated for the same commit after this release selects DIFFERENT probes than one generated before it. Deliberate: the pre-fix selection was not reproducible from the disclosed seed at all. (3) check-template-sync gained a dependency on two modules it did not import before; the import is guarded and degrades to a reported skip, verified by deleting verification-pack.mjs from a tree that has a library. (4) A `--seed` longer than 12 characters is now used verbatim rather than truncated, so it appears in full in the reproduce command — correct, if less tidy. (5) INV-29's retirement leaves a numbering gap, which is intended (INV-59: stability beats density).
INVARIANTS AT RISK: INV-58 (the case set changed — one removed, three added, INV-66 gained a second; re-proven 69/69 across 78 mutations), INV-26 (library grew to 69, all runnable, 0 manual — re-run PASS), INV-52 (buildPack restructured; its test extended and re-proven), INV-56 (check strengthened, still passing, mutation re-proven), INV-50/INV-66 (text changed, Verify unchanged, both mutation-proven), INV-23 (VERSION 1.31.0 and the newest CHANGELOG heading match), INV-22 (guard.test.mjs green — its sandbox has no .cycle/, so check 12 skips there by design), INV-01/INV-17 (CLAUDE.md untouched, gen --check green), INV-04/INV-05 (only a CSS comment changed in the console). INV-29 is RETIRED deliberately. None failing.
NET SCORE: 1 − 0 = 1 (production fix: B2/INV-70 — this one actually fired, in this cycle: the §4v pack handed to the verifier printed a reproduce command that returned a different five invariants, and the verifier followed it. Defensive/structural: 3 — A1, A2 and B1 close false-green CLASSES whose trigger has not yet fired on this tree, which is exactly what defensive_count exists to make visible without loosening the net-score gate.)

OPERATOR ACTIONS / DEPLOY:
- None for consumers: no command body, prompt or config-schema change, so no /sync-commands re-pull | BLOCKS DEPLOY: N
- Any §4v pack generated BEFORE v1.31.0 has an unreproducible probe list — regenerate rather than trusting its `--seed` line | BLOCKS DEPLOY: N
Deploy: Interactive Console (HTML): merge to `main` — GitHub Pages republishes from index.html (only a CSS comment changed). Canonical Templates & Docs: no deploy. Tooling & Sync Infrastructure: no deploy (CI-only).

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- Batches 3–5 of the plan remain: INV-71 (PROJECT_HEALTH label contract derived across its four readers), INV-74 (CI runs every Test Command stage), INV-72 + backfilling a block for v1.30.1, and the docs/state hygiene batch.
- The seam counter in .cycle/STATE.md still reads 3/3 DUE even though the Seams audit HAS run. Resetting it to 0 is Batch 5 item E2 and was deliberately left out of this scope; until it lands, /audit and portfolio-status will keep reporting a Seams audit as due.
- INV-73 (§1s asking about the project's CONFIGURED Axis B categories) stays deferred pending a decision — closing it means editing an --assert-locked canonical body AND the SEAMS block's registered field names, and making those names dynamic weakens the shape check that guards them.
- The §4v-vs-SessionStart-hook independence question is unresolved and is a workflow-design decision, not a bug fix.
- Unchanged from earlier batches: deleteProject() leaves per-project keys behind; the project form mis-parses rule text containing a pipe; --accent is 3.4:1 on the light surface; the phase-dot label is 2.3:1; legacyCopy()'s success path is unexercised.

DOCUMENTATION UPDATES NEEDED:
- .cycle/config.md Common Gotchas: two from this batch — a guard that proves a covering rule EXISTS must also prove it APPLIES (the property coverage rests on is often unstated and therefore unguarded); and TWO parsers of one file is a seam, so measure both against the file rather than against each other's output. Plus the one still owed from v1.30.0: a stubbed DOM that is a flat id→element map cannot represent "getElementById returns the first match".
- README line 122 describes the pack's probes as "seeded from the commit sha, so … a verifier can reproduce the selection". That claim was FALSE before this release and is TRUE now; no edit is required, but it is worth stating that the DISCLOSED seed is the one used.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
