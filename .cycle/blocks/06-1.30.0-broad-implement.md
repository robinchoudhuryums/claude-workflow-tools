---BROAD SCAN IMPLEMENTATION SUMMARY---
NOTE ON ORDER: this block is POST-REFLECT remediation. `06-a-reflect.md` closed Cycle 6 by carrying two
live defects forward "UNFIXED"; this block closes them. Read this one as superseding that note. The reflect
block is deliberately left intact rather than edited — it is the honest record of what that reflection
concluded with the information it had.

Findings implemented: the two duplicate-id collisions found by /regression (Tier 1 panel destroyed on load; Setup Copy copying the section), plus INV-67 to make the class impossible
Files modified: claude-code-guide-v2.html, scripts/gen-html-prompts.mjs, scripts/check-html.mjs, tests/mutation-audit.mjs, .cycle/config.md, CHANGELOG.md, VERSION

CHANGES:
DUP-1 (High) | claude-code-guide-v2.html (markup + renderTier1) | `<section id="t1">` and `<pre id="t1">` shared an id. getElementById returns the FIRST element in document order, so renderTier1() assigned textContent to the SECTION — destroying its header, both prompts, both Copy buttons and both Fill buttons on every load and replacing them with unstyled, unwrapped prose; `<pre id="t1i">` (Broad Implement) was destroyed with it. Fixed by renaming the PRE to `t1a` (matching the existing t2a/t2b pattern) and its five references (block-t1a, copy-t1a, fill-t1a, toggleFill, doCopy, the two getElementById/querySelector calls in renderTier1). The SECTION id is untouched — nav hrefs, showPanel() and handleHash() depend on it.
DUP-2 (Medium) | claude-code-guide-v2.html (markup), scripts/gen-html-prompts.mjs (MANIFEST) | same collision on `setup`: doCopy('setup') read the SECTION, so the panel heading, purpose blurb and warning note were pasted into the prompt handed to an agent. Pre renamed to `psetup` (matching the p* convention); MANIFEST id updated. Copy is now 10,127 chars — exactly the prompt — down from 10,841.
INV-67 | scripts/check-html.mjs, .cycle/config.md, tests/mutation-audit.mjs | strip the <script> block, collect every markup id, fail on a duplicate. 177 ids checked. Mutation-proven by reinstating the exact collision (`<pre id="t1a">` → `<pre id="t1">`).

TEST RESULTS: passed — full 16-stage Test Command green (292 ✓); invariant-check 67/67 runnable PASS, 0 manual; mutation audit 67/67 proven fail-closed across 75 mutations. Browser (headless Chromium): the Tier 1 section now has 5 child elements, a header, 2 Copy buttons and both prompts as real <pre> blocks (t1a 9,452 chars; t1i 4,586); all 25 <pre id> render non-empty; getFilledText('psetup') returns exactly the 10,127-char prompt starting "Do not make any changes to any files during this session."; zero console errors. Screenshot confirms the panel renders with monospace formatting and preserved ASCII rules.
REGRESSION RISKS: (1) NO stored fill values are orphaned by the rename — the Tier 1 fill form lived inside the destroyed subtree so it could never have run and saved a `ccg:*:t1:*` key, and Setup has no fill form. (2) Section ids are unchanged, so every nav href, deep link and showPanel/handleHash path is untouched (verified: 20 panels, 20 nav links, all resolving). (3) The Tier 1 fill form now renders "No fillable placeholders in this prompt." — correct, not a defect: /broad-scan's bracket tokens are all output-format tokens under the F11 classification. (4) MANIFEST id `setup` → `psetup` is internal to gen-html-prompts; --assert and --write both re-verified.
INVARIANTS AT RISK: INV-50 (unchanged and still passing, but this cycle proved its scope is narrower than its wording — it proves an id EXISTS, not that the element resolved at runtime is the intended one; INV-67 is the complement, not a replacement), INV-29/INV-36 (the lock still covers all 16 static and 9 dynamic prompts after the id rename; --assert green), INV-20/INV-53 (the hostile fixture derives sinks and fill ids from the markup, so it followed the rename automatically), INV-63 (the F11 classification derives from `id="fill-*"` and picked up fill-t1a with no edit). None failing.
NET SCORE: 2 − 0 = 2 (production fixes DUP-1 High, DUP-2 Medium; defensive/structural: 1, INV-67 itself)

BOOKKEEPING FOR §6a: /reflect has ALREADY run for Cycle 6 and is the sole writer of
net_score/prod_fixes/new_failure_modes, so these two fixes are NOT in the cycle-6 metrics row. That row
reads 13 − 0; the true cycle total is 15 − 0 with 5 defensive. No metrics row was added here, following
the Cycle-5 precedent for post-synthesis remediation.

OPERATOR ACTIONS / DEPLOY:
- None for consumers: console + guard only, no command body or config-schema change | BLOCKS DEPLOY: N
- Walk the Tier 1 panel once Pages republishes — it has been broken for many releases, so it is worth seeing it render for real | BLOCKS DEPLOY: N
Deploy: Interactive Console (HTML): merge to `main` — GitHub Pages republishes from index.html; browser-verify AFTER. Canonical Templates & Docs: no deploy. Tooling & Sync Infrastructure: no deploy (CI-only).

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- INV-50's wording overstates what it proves. Either reword it to its true scope ("every id a render writes to is present in the markup") or give the stub document order so it can catch shadowing directly. INV-67 covers the duplicate case; a render targeting a WRONG-but-unique id is still only caught by the phantom check.
- ROADMAP R20 (real-DOM console test stage) is now doubly earned: this defect class was invisible to the vm harness and both instances were found by driving Chromium from a session scratchpad.
- Carried, unaddressed: deleteProject() leaves per-project keys behind; the project form mis-parses rule text containing a pipe; --accent is 3.4:1 on the light surface; the phase-dot label is 2.3:1; legacyCopy()'s success path is unexercised.

DOCUMENTATION UPDATES NEEDED:
- .cycle/config.md Common Gotchas: one more from this fix — a stubbed DOM that is a flat id→element map cannot represent "getElementById returns the first match", so id shadowing is invisible to it BY CONSTRUCTION; the markup, not the harness, is where that class has to be checked.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
