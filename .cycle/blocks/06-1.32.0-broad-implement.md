---BROAD SCAN IMPLEMENTATION SUMMARY---
NOTE ON ORDER: post-reflect, post-§4v, post-Seams remediation — Batches 3, 4 and 5 of the plan built
from the §4v VERIFICATION BLOCK (.cycle/blocks/06-b-verification.md) and the SEAMS & INVARIANTS AUDIT
BLOCK (06-c-seams.md). /reflect already ran for Cycle 6 and is the sole writer of
net_score/prod_fixes/new_failure_modes, so nothing here is in the cycle-6 metrics row.

Findings implemented: C1 (INV-71 one definition of the PROJECT_HEALTH "Current Standing" label contract), C2 (INV-74 CI runs every stage of the documented Test Command, in order), D1 (three missing blocks backfilled into .cycle/blocks/), D2 (INV-72 every release of the current cycle has a block), E2 (Seams counter reset to 0), E3 (VERSION + CHANGELOG)
Files modified: scripts/health-fields.mjs (new), scripts/portfolio.mjs, scripts/portfolio-status.mjs, scripts/check-html.mjs, scripts/check-template-sync.mjs, tests/mutation-audit.mjs, .cycle/config.md, .cycle/STATE.md, .cycle/blocks/06-1.30.1-broad-implement.md (new), .cycle/blocks/06-b-verification.md (new), .cycle/blocks/06-c-seams.md (new), CHANGELOG.md, VERSION

CHANGES:
C1 | scripts/health-fields.mjs (new), scripts/portfolio.mjs, scripts/portfolio-status.mjs, scripts/check-html.mjs, scripts/check-template-sync.mjs (structural check 13) | The five "Current Standing" labels were hard-coded independently in four artifacts and matched only by luck. health-fields.mjs is now the one definition; both portfolio readers import it. It is KEYED, not an array — a positional destructure would have silently re-mapped every reader's fields if the list were ever reordered, which would have been a new failure mode introduced by the fix. The copies that cannot import it are checked against it INSIDE THE SPAN THAT WRITES THEM (the /cycle-init skeleton in CLAUDE.md, the generated command file, the live PROJECT_HEALTH.md), never file-globally: every label appears twice per artifact, so a whole-file `includes` would pass with the skeleton broken — the file-global marker gotcha this repo already documented. An anchor that stops matching FAILS rather than silently checking nothing. The CONSOLE is verified by BEHAVIOUR instead: parseHealth matches `Overall[^:\n]*:` rather than spelling the label, so a literal scan failed there even though the parser is correct; check-html now builds a Current Standing block FROM the canonical labels and requires every field to parse. That is the actual contract and it survives any rewording of the regexes.
C2 | scripts/check-template-sync.mjs (structural check 14) | Parses the stage list out of the documented Test Command and out of .github/workflows/sync-check.yml and requires them equal AND in the same order. Reports missing stages, extra stages, and same-set-different-order separately. Skips where either side is absent or the Test Command names no script stages (a consuming project running `npm test` has nothing to compare). Confirmed exact at 16 stages.
D1 | .cycle/blocks/06-1.30.1-broad-implement.md, 06-b-verification.md, 06-c-seams.md | Three blocks that existed nowhere §4v or §6a can read. The v1.30.1 block is explicitly marked a BACKFILL and is not presented as a transcript: its claims were re-verified in this session (all 17 removed classes have zero class-attribute uses; the browser probes are byte-identical to v1.30.0) rather than copied from the commit message. The §4v and Seams blocks are verbatim from the 2026-09-04 session that produced them.
D2 | scripts/check-template-sync.mjs (structural check 15) | Derives the cycle number from STATE.md, derives a FLOOR from the lowest version this cycle already has a block for (so the rule never reaches back into cycles predating the convention), takes VERSION as the ceiling, and fails naming any release in that range with no block.
E2 | .cycle/STATE.md | Seams counter 3 → 0, reset by the Seams & Invariants audit of 2026-09-04, with a pointer to its block.
E3 | VERSION, CHANGELOG.md | 1.32.0.

TEST RESULTS: passed — full 16-stage Test Command green, 304 ✓ (was 298); invariant-check 72/72 runnable PASS, 0 manual; mutation audit 72/72 proven fail-closed across 81 mutations (was 78). Each new guard was proven against its own vector: drifting a label in health-fields.mjs (INV-71 fails), dropping a CI stage the Test Command still documents (INV-74 fails), and inserting a CHANGELOG release with no block (INV-72 fails). TWO of these guards were WRONG on first write and both were caught before landing. (a) Check 13 originally scanned whole FILES for the labels, which would have passed with the console's parseHealth broken — every label appears twice per artifact, so a file-global `includes` proves nothing; scoping each check to the span that writes it, and moving the console to a behavioural check, is the corrected form. (b) Check 13 then shadowed an outer `absent` with an inner one — a TDZ that fires ONLY when a target file is missing, so it was invisible in this repo and caught by guard.test.mjs's sandbox, which is precisely the case that sandbox exists for.
REGRESSION RISKS: (1) health-fields.mjs is a new module imported by portfolio.mjs, portfolio-status.mjs, check-html.mjs and (dynamically) check-template-sync.mjs. A consuming project that copies some scripts but not this one would break the two portfolio scripts at import; the check-template-sync path is already guarded and degrades to a reported skip. (2) INV-72's floor is derived from existing blocks, so a cycle whose FIRST release ships with no block sets its own floor above that release and the gap goes unnoticed until a later release is blocked. The check is a ratchet, not a proof of completeness — worth stating plainly rather than overselling. (3) INV-74 compares only `node scripts/…` and `node tests/…` stages; a Test Command using a different runner is skipped, not failed. (4) The backfilled v1.30.1 block is a reconstruction and is labelled as one; §6a should weight it accordingly.
INVARIANTS AT RISK: INV-27/INV-35 (both portfolio readers rewired to the shared definition — behaviour unchanged, both test suites green), INV-37 (the parseHealth fixture is now derived from HEALTH_FIELDS rather than hand-written; strictly stronger, re-proven), INV-58 (three cases added; re-proven 72/72 across 81 mutations), INV-26 (library grew to 72, all runnable, 0 manual), INV-68 (the parse floor still holds at 72), INV-23 (VERSION 1.32.0 matches the newest CHANGELOG heading), INV-22 (guard.test.mjs green — its sandbox has no .cycle/, so checks 12/15 skip there by design), INV-14 (unchanged; INV-74 is strictly stronger and does not replace it). None failing.
NET SCORE: 0 − 0 = 0 (defensive/structural: 6 — C1, C2, D1, D2, E2, E3. Nothing here fixes a live production defect: the label contract MATCHED, CI parity WAS exact, and the missing blocks were a verification-visibility gap rather than a behaviour bug. Every item closes a class whose trigger has not fired on this tree, which is exactly what defensive_count exists to make visible without loosening the net-score gate. The honest read of this batch is that it is insurance, and it should not be counted as anything else.)

OPERATOR ACTIONS / DEPLOY:
- None for consumers: no command body, prompt or config-schema change, so no /sync-commands re-pull | BLOCKS DEPLOY: N
- Consuming projects that copy individual scripts: scripts/health-fields.mjs is now required by portfolio.mjs and portfolio-status.mjs | BLOCKS DEPLOY: N
Deploy: Interactive Console (HTML): no console change in this release. Canonical Templates & Docs: no deploy. Tooling & Sync Infrastructure: no deploy (CI-only).

(Not complete in production until blocking operator actions are done AND
the deploy step is confirmed.)

FOLLOW-ON ITEMS:
- §6a HEALTH SYNTHESIS is the only step left in Cycle 6. Its inputs are the eight .cycle/blocks/06-*.md files, which now include the §4v and Seams blocks.
- INV-73 (§1s must ask about the project's CONFIGURED Axis B categories) remains OPEN pending a decision, not work: closing it means editing an --assert-locked canonical body AND the SEAMS block's registered field names, and making those names dynamic weakens the shape check that guards them.
- The §4v-vs-SessionStart-hook independence question remains OPEN pending a decision: the hook injects implementer prose into every new session, including a §4v one, contradicting §4v's own opening instruction.
- INV-72's ratchet limitation (risk 2 above) is worth a second look in Cycle 7 — a cycle's first release is the one case it cannot see.
- Unchanged from earlier batches: deleteProject() leaves per-project keys behind; the project form mis-parses rule text containing a pipe; --accent is 3.4:1 on the light surface; the phase-dot label is 2.3:1; legacyCopy()'s success path is unexercised.

DOCUMENTATION UPDATES NEEDED:
- Common Gotchas (three owed, to be applied by /sync-docs immediately after this batch): a stubbed DOM that is a flat id→element map cannot represent "getElementById returns the first match" (owed since v1.30.0); a guard that proves a covering rule EXISTS must also prove it APPLIES; and TWO parsers of one file is a seam — measure both against the file, not against each other.
- README: scripts/health-fields.mjs joins the script inventory.
- .cycle/config.md Subsystems: scripts/health-fields.mjs joins the Tooling subsystem file list.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
