---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: F01 (Critical — GitHub token serialized into state backups
and into .cycle/console-state.json), F04 (High — stored content unescaped at six
innerHTML/attribute sinks), F05 (Medium — copy failed silently), F08 (Medium —
tabbed navigation had no headless coverage).
Files modified: claude-code-guide-v2.html, scripts/check-html.mjs, .gitignore
(new), .cycle/config.md, .cycle/STATE.md, README.md, CHANGELOG.md, VERSION
CHANGES:
F01 | SECRET_KEYS/isSecretKey(); secrets excluded from collectState() AND
      stateBackupKeys(); ccg:secret:* deny-by-default; UI claim corrected;
      .cycle/console-state.json gitignored.
F04 | Six sinks fixed: renderCycle (label + advancePhase + setPhase args),
      renderSubsysTable, renderT2SubsysTable, renderProjectSelector,
      renderCustomInvariantsList, dashboardCard (href + two handler args).
F05 | Single copyToClipboard() with availability guard, .catch(), execCommand
      fallback, visible "Copy failed" state.
F08 | Real panel/nav fixtures replacing the [] stub; four showPanel/handleHash
      assertions.
TEST RESULTS: passed — 13 stages exit 0; invariant-check 29/29 runnable.
REGRESSION RISKS: restoring state on a new machine no longer carries the token
(intended). legacyCopy() fallback path is code-read verified only.
INVARIANTS AT RISK: None violated. INV-09 and INV-20 REWRITTEN — both were the
invariants that permitted these bugs. INV-40/41/42 added.
NET SCORE (self-reported): 4 − 0 = 4   [LATER CORRECTED BY /reflect TO 3 — F08 is defensive]
---END BROAD SCAN IMPLEMENTATION SUMMARY---
